/**
 * An element that can be faded.
 */
export interface IFadeableElement {
  /**
   * Fade in time in ms.
   */
  fadeInTimeMs: number;

  /**
   * Fade out time in ms.
   */
  fadeOutTimeMs: number;
}